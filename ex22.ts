let data: string[] = ["daniyal", "omair", "junaid", "ali", "hassan"]

// Intentional error: accessing an index beyond the length of the array
console.log(data[5])

// Correcting the error by accessing a valid index
console.log(data[2])