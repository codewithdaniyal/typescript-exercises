function make_shirt(size: string, message: string): void {
    console.log(`The shirt size is ${size} and it has the message: "${message}"`);
}

// Call the function with example arguments
make_shirt('Medium', 'The T-Shirt is Medium!');